rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open access to all documents (WARNING: public read/write)
    match /{document=**} {
      allow read: if true;
      allow write: if true;
    }
  }
}