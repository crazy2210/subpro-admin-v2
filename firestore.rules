rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public access: allow anyone to read and write any document
    match /{document=**} {
      allow read, write: if true;
    }
  }
}