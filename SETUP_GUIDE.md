# 🚀 دليل الإعداد والتشغيل الشامل
## SubPro Dashboard - Setup & Installation Guide

---

## 📋 نظرة عامة

هذا الدليل يشرح بالتفصيل كيفية إعداد وتشغيل نظام SubPro Dashboard مع جميع التحديثات والميزات الجديدة.

---

## ✅ المتطلبات الأساسية

### 1. البرامج المطلوبة
- متصفح حديث (Chrome, Firefox, Safari, Edge)
- محرر نصوص (VS Code, Sublime Text, أو أي محرر آخر)
- اتصال بالإنترنت

### 2. حسابات مطلوبة
- حساب Firebase (مجاني)
- حساب Google (للربط مع Google Sheets)

---

## 🔧 خطوات الإعداد

### الخطوة 1: إعداد Firebase

#### 1.1 إنشاء مشروع Firebase
1. اذهب إلى [Firebase Console](https://console.firebase.google.com)
2. اضغط على "Add project" أو "إضافة مشروع"
3. أدخل اسم المشروع (مثلاً: SubPro-Dashboard)
4. اتبع الخطوات حتى يتم إنشاء المشروع

#### 1.2 تفعيل Firestore Database
1. من القائمة الجانبية، اختر "Firestore Database"
2. اضغط "Create database"
3. اختر "Start in production mode"
4. اختر موقع الخادم (اختر الأقرب لك)
5. اضغط "Enable"

#### 1.3 تفعيل Authentication
1. من القائمة الجانبية، اختر "Authentication"
2. اضغط "Get started"
3. من تبويب "Sign-in method"
4. فعّل "Email/Password"

#### 1.4 الحصول على إعدادات Firebase
1. اذهب إلى إعدادات المشروع (أيقونة الترس بجانب "Project Overview")
2. انزل لأسفل حتى "Your apps"
3. اضغط على أيقونة الويب `</>`
4. أدخل اسم التطبيق واضغط "Register app"
5. انسخ كود `firebaseConfig`

#### 1.5 تحديث إعدادات Firebase في المشروع
1. افتح ملف `app.js`
2. ابحث عن `firebaseConfig`
3. استبدل القيم بالقيم التي نسختها:

```javascript
const firebaseConfig = {
    apiKey: "YOUR_API_KEY",
    authDomain: "YOUR_AUTH_DOMAIN",
    projectId: "YOUR_PROJECT_ID",
    storageBucket: "YOUR_STORAGE_BUCKET",
    messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
    appId: "YOUR_APP_ID"
};
```

### الخطوة 2: إعداد قواعد Firestore

#### 2.1 نسخ قواعد الأمان
1. في Firebase Console، اذهب إلى "Firestore Database"
2. اختر تبويب "Rules"
3. انسخ المحتوى التالي:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // قواعد المبيعات
    match /sales/{saleId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
    
    // قواعد الحسابات
    match /accounts/{accountId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
    
    // قواعد المصروفات
    match /expenses/{expenseId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
    
    // قواعد المنتجات
    match /products/{productId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
    
    // قواعد المشاكل
    match /problems/{problemId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
    
    // قواعد الحملات الإعلانية
    match /ad_campaigns/{campaignId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
    
    // قواعد التنبيهات - جديد
    match /notifications/{notificationId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
    
    // قواعد سجل العمليات - جديد
    match /audit_logs/{logId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
    
    // قواعد المستخدمين
    match /users/{userId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
  }
}
```

4. اضغط "Publish"

### الخطوة 3: إنشاء مستخدم أولي

#### 3.1 إضافة مستخدم في Authentication
1. في Firebase Console، اذهب إلى "Authentication"
2. اختر تبويب "Users"
3. اضغط "Add user"
4. أدخل البريد الإلكتروني وكلمة المرور
5. اضغط "Add user"

#### 3.2 إضافة صلاحيات المستخدم في Firestore
1. اذهب إلى "Firestore Database"
2. اضغط "Start collection"
3. اسم المجموعة: `users`
4. Document ID: استخدم نفس UID المستخدم من Authentication
5. أضف الحقول التالية:
   - `email`: البريد الإلكتروني
   - `role`: `admin`
   - `displayName`: اسم المستخدم
   - `createdAt`: اختر Timestamp واضغط على الوقت الحالي

### الخطوة 4: إعداد Google Sheets Integration (اختياري)

#### 4.1 إنشاء Google Sheet
1. اذهب إلى [Google Sheets](https://sheets.google.com)
2. أنشئ شيت جديد
3. سمّه "SubPro Orders" أو أي اسم تريده

#### 4.2 إعداد Google Apps Script
1. في Google Sheet، اذهب إلى Extensions > Apps Script
2. احذف الكود الموجود
3. انسخ محتوى ملف `google-apps-script.gs` والصقه
4. اضغط على أيقونة القرص (Save)

#### 4.3 نشر Web App
1. اضغط على "Deploy" > "New deployment"
2. اختر نوع "Web app"
3. في "Execute as": اختر "Me"
4. في "Who has access": اختر "Anyone"
5. اضغط "Deploy"
6. انسخ "Web App URL"

#### 4.4 تحديث إعدادات المشروع
1. افتح ملف `google-sheets-integration.js`
2. ابحث عن `GOOGLE_SHEETS_CONFIG`
3. حدّث القيم:

```javascript
const GOOGLE_SHEETS_CONFIG = {
    spreadsheetId: 'YOUR_SPREADSHEET_ID', // من رابط الشيت
    sheetName: 'Orders',
    webAppUrl: 'YOUR_WEB_APP_URL', // اللي نسخته من الخطوة السابقة
    enabled: true // غيرها من false إلى true
};
```

### الخطوة 5: رفع الملفات

#### 5.1 رفع على استضافة ويب
إذا كنت تريد استخدام استضافة ويب عادية:
1. ارفع جميع الملفات إلى الاستضافة
2. تأكد أن `index.html` في المجلد الرئيسي

#### 5.2 استخدام Firebase Hosting (موصى به)
1. ثبّت Firebase CLI:
```bash
npm install -g firebase-tools
```

2. سجل الدخول:
```bash
firebase login
```

3. هيئ المشروع:
```bash
firebase init hosting
```

4. اختر المشروع الذي أنشأته
5. اختر المجلد العام: اضغط Enter (سيكون `public`)
6. هل تريد SPA؟ اختر "No"

7. انسخ جميع الملفات إلى مجلد `public`

8. انشر المشروع:
```bash
firebase deploy
```

### الخطوة 6: إضافة المكونات الجديدة للمشروع

#### 6.1 تحديث ملف index.html
افتح `index.html` وأضف قبل نهاية `</body>`:

```html
<!-- المكونات الجديدة -->
<script type="module" src="subscriptions-system.js"></script>
<script type="module" src="google-sheets-integration.js"></script>
<script type="module" src="new-components.js"></script>
```

#### 6.2 إضافة HTML المكونات الجديدة
افتح `index.html` وأضف محتوى ملف `new-components.html` قبل نهاية `</body>`

---

## 🎯 اختبار النظام

### 1. تسجيل الدخول
1. افتح `login.html` في المتصفح
2. أدخل البريد الإلكتروني وكلمة المرور
3. اضغط "تسجيل الدخول"

### 2. إضافة منتج
1. من لوحة التحكم، اضغط "إضافة منتج"
2. أدخل اسم المنتج
3. اضغط "إضافة"

### 3. إضافة أوردر
1. اذهب إلى "سجل المبيعات"
2. اضغط "إضافة أوردر"
3. املأ البيانات
4. اضغط "حفظ الأوردر"

### 4. اختبار التجديد
1. في نموذج إضافة أوردر، اضغط "تجديد"
2. ابحث عن عميل موجود
3. اختر الأوردر
4. سيتم ملء البيانات تلقائياً

### 5. اختبار التنبيهات
1. أضف أوردر بتاريخ انتهاء قريب
2. انتظر دقيقتين
3. افتح لوحة التنبيهات
4. يجب أن تظهر تنبيهات الاشتراكات القريبة من الانتهاء

### 6. اختبار Google Sheets
1. أضف أوردر جديد
2. افتح Google Sheet
3. يجب أن يظهر الأوردر تلقائياً

---

## 🔍 استكشاف الأخطاء

### مشكلة: لا يمكن تسجيل الدخول
**الحل:**
- تأكد من تفعيل Email/Password في Firebase Authentication
- تأكد من إضافة المستخدم في قسم Users
- تحقق من صحة البريد الإلكتروني وكلمة المرور

### مشكلة: لا تظهر البيانات
**الحل:**
- افتح Console في المتصفح (F12)
- تحقق من وجود أخطاء
- تأكد من صحة قواعد Firestore
- تأكد من تسجيل الدخول

### مشكلة: لا يعمل Google Sheets
**الحل:**
- تأكد من نشر Web App بشكل صحيح
- تحقق من صحة Web App URL
- تأكد من تفعيل الربط (`enabled: true`)
- تحقق من Console للأخطاء

### مشكلة: لا تظهر التنبيهات
**الحل:**
- تأكد من وجود أوردرات بتاريخ انتهاء قريب
- انتظر 2-5 دقائق (الفحص التلقائي كل 5 دقائق)
- تحقق من Console للأخطاء
- تأكد من إضافة مجموعة `notifications` في Firestore

---

## 📊 الميزات الجديدة

### ✅ نظام التجديدات
- تجديد يدوي من زر "تجديد"
- بحث بالاسم، الهاتف، الإيميل، أو رقم الأوردر
- ربط تلقائي بالأوردر القديم
- تجديد تلقائي (قريباً)

### ✅ تتبع الدفع
- حالات: مدفوع، مدفوع جزئياً، غير مدفوع
- حساب تلقائي للمبلغ المتبقي
- تنبيهات للمبالغ المتبقية

### ✅ الاشتراكات طويلة المدة
- دعم اشتراكات سنوية، نصف سنوية، ربع سنوية
- تبديل الحساب شهرياً
- تنبيهات تلقائية لموعد التبديل

### ✅ استبدال الحسابات التالفة
- زر "وسم كتالف واستبدال"
- اختيار تلقائي لحساب بديل
- سجل كامل للاستبدالات
- عرض مميز للحسابات (أحمر للتالف، أخضر للبديل)

### ✅ إحصائيات الشيفتات
- 3 شيفتات: صباحي، مسائي، ليلي
- عدد الأوردرات لكل شيفت
- تفصيل المنتجات داخل كل شيفت
- تصدير النتائج

### ✅ نظام المصروفات المحسّن
- إضافة تاريخ لكل مصروف إعلان
- تعديل التاريخ
- حساب تكلفة الأوردر الواحد
- إحصائيات يومية

### ✅ ربط Google Sheets
- إرسال تلقائي عند إضافة أوردر
- تحديث عند التعديل
- سجل كامل في الشيت

---

## 📝 ملاحظات مهمة

### الأمان
- لا تشارك إعدادات Firebase مع أحد
- استخدم كلمات مرور قوية
- فعّل Two-Factor Authentication في Google

### الأداء
- النظام يدعم آلاف الأوردرات
- استخدم الفلاتر لتحسين الأداء
- نظف البيانات القديمة بانتظام

### النسخ الاحتياطي
- استخدم "تصدير شامل" بانتظام
- احتفظ بنسخة من قاعدة البيانات
- صدّر Google Sheets كنسخة احتياطية

---

## 🆘 الدعم الفني

إذا واجهت أي مشاكل:
1. تحقق من قسم "استكشاف الأخطاء" أعلاه
2. افتح Console في المتصفح (F12) وتحقق من الأخطاء
3. راجع ملفات التوثيق الأخرى في المشروع

---

## 🎉 تهانينا!

إذا وصلت هنا، فقد أكملت إعداد نظام SubPro Dashboard بنجاح! 

استمتع باستخدام النظام وإدارة اشتراكاتك بكفاءة! 🚀
