# 📚 توثيق الميزات المحسنة - SubPro Dashboard V3

## 🎯 نظرة عامة

تم تطوير مجموعة شاملة من الميزات المحسنة لنظام SubPro Dashboard لتوفير إدارة احترافية للاشتراكات، الأكونتات، والمصروفات مع تقارير دقيقة وواجهات مستخدم محسّنة.

---

## 📦 الملفات الجديدة

### 1. `enhanced-features.js`
الملف الأساسي الذي يحتوي على جميع الوظائف المحسنة:
- إدارة الشيفتات (3 شيفتات يومية)
- إدارة المصروفات مع فلاتر متقدمة
- إدارة الأكونتات (available/used/damaged)
- منطق استبدال الأكونت التالف تلقائياً
- إحصائيات المنتجات التفصيلية
- أدوات التصدير والمساعدة

### 2. `order-details.html`
صفحة تفاصيل الأوردر المحسنة:
- واجهة شاملة لعرض تفاصيل الأوردر
- بطاقات منظمة (ملخص الأوردر، الحساب المرتبط، الاشتراك)
- خط زمني (Timeline) للأحداث
- إمكانية تعيين الحساب كـ تالف واستبداله
- أزرار النسخ السريع للبيانات

### 3. `enhanced-sections.html`
الأقسام المحسنة للواجهة:
- قسم المصروفات المحسن
- قسم الشيفتات التفصيلي
- قسم إحصائيات المنتجات
- قسم إدارة الأكونتات المتقدم
- الصفحة الرئيسية المحسنة

### 4. `enhanced-integration.js`
ملف الربط والتكامل:
- دمج الميزات الجديدة مع النظام الموجود
- إدارة الحالة (State Management)
- معالجة الأحداث (Event Handlers)
- تحديث الواجهات ديناميكياً

---

## 🚀 الميزات الرئيسية

### 1️⃣ نظام الشيفتات المتقدم

#### التعريف
```javascript
// 3 شيفتات يومية ثابتة
EVENING: 16:00 → 23:59:59 (شيفت العصر)
NIGHT:   00:00 → 07:59:59 (شيفت الليل)
MORNING: 08:00 → 15:59:59 (شيفت الصبح)
```

#### الإحصائيات المحسوبة لكل شيفت:
- ✅ عدد الأوردرات داخل نطاق الشيفت
- ✅ عدد الأكونتات المستخدمة
- ✅ تفصيل أوردرات كل منتج
- ✅ إجمالي الإيرادات والأرباح

#### الاستخدام:
```javascript
import { calculateShiftStatistics } from './enhanced-features.js';

const targetDate = new Date('2025-11-11');
const stats = await calculateShiftStatistics(db, targetDate);

console.log(stats.shifts.EVENING.totalOrders); // عدد أوردرات شيفت العصر
console.log(stats.shifts.EVENING.productBreakdown); // تفصيل المنتجات
```

#### التصدير:
- **CSV**: نص منظم يمكن فتحه في Excel
- **Excel**: ملف XLSX احترافي مع تنسيق

---

### 2️⃣ إدارة المصروفات المحسنة

#### الميزات:
- ✅ حقل تاريخ إلزامي (ISO format)
- ✅ قبول تواريخ قديمة أو مستقبلية ضمن حدود منطقية
- ✅ فرز افتراضي تنازلي (الأحدث أولاً)
- ✅ إمكانية التبديل لتصاعدي

#### فئات المصروفات:
```javascript
const EXPENSE_CATEGORIES = {
    ADS: 'إعلانات',
    OPERATIONAL: 'تشغيلي',
    RENEWAL: 'تجديد',
    OTHER: 'أخرى'
};
```

#### الفلاتر المتاحة:
- 📅 نطاق التاريخ (Date Range)
- 📦 المنتج (Product)
- 🏷️ نوع المصروف (Category)
- 🔄 ترتيب (Sort Order)

#### التجميعات:
```javascript
import { aggregateExpenses } from './enhanced-features.js';

// تجميع يومي
const dailyAgg = aggregateExpenses(expenses, 'daily');

// تجميع أسبوعي
const weeklyAgg = aggregateExpenses(expenses, 'weekly');

// تجميع شهري
const monthlyAgg = aggregateExpenses(expenses, 'monthly');
```

#### التحقق من التاريخ:
```javascript
import { addExpenseWithDate } from './enhanced-features.js';

try {
    await addExpenseWithDate(db, {
        date: '2025-11-11T10:30:00',
        amount: 500,
        category: 'ads',
        product_id: 'Netflix',
        note: 'حملة إعلانية على فيسبوك'
    });
} catch (error) {
    console.error('خطأ في إضافة المصروف:', error.message);
}
```

---

### 3️⃣ إحصائيات المنتجات التفصيلية

#### المعلومات المعروضة لكل منتج:
- 📊 عدد الأوردرات
- 💰 إجمالي الإيرادات
- 📈 إجمالي الأرباح
- 💵 متوسط سعر الأوردر
- 📉 نسبة المساهمة من إجمالي المبيعات
- 📊 اتجاه المبيعات (Trend)

#### الاستخدام:
```javascript
import { calculateProductStatistics } from './enhanced-features.js';

const stats = await calculateProductStatistics(
    db, 
    'Netflix', 
    '2025-01-01', 
    '2025-11-11'
);

console.log(`الأوردرات: ${stats.totalOrders}`);
console.log(`الإيرادات: ${stats.totalRevenue} ريال`);
console.log(`المساهمة: ${stats.contributionPercentage}%`);
console.log(`الاتجاه: ${stats.salesTrend}`); // increasing/stable/decreasing
```

#### المخططات البيانية:
- **مخطط أعمدة**: مقارنة الإيرادات والأرباح لكل منتج
- **بطاقات إحصائية**: معلومات تفصيلية لكل منتج

---

### 4️⃣ إدارة الأكونتات المتقدمة

#### حالات الأكونت:
```javascript
const ACCOUNT_STATUS = {
    AVAILABLE: 'available',  // متاح
    USED: 'used',            // مستخدم
    DAMAGED: 'damaged'       // تالف
};
```

#### الفلاتر:
- ✅ حالة الحساب (available/used/damaged)
- ✅ المنتج
- ✅ بحث نصي (البريد أو المعرّف)

#### العدادات:
```javascript
import { getAccountsStatistics } from './enhanced-features.js';

const stats = await getAccountsStatistics(db);

console.log(`إجمالي: ${stats.total}`);
console.log(`متاح: ${stats.available}`);
console.log(`مستخدم: ${stats.used}`);
console.log(`تالف: ${stats.damaged}`);

// إحصائيات حسب المنتج
console.log(stats.byProduct);
```

#### تنبيهات النقص:
- 🚨 تنبيه تلقائي عند نقص الأكونتات المتاحة
- 🔢 حد أدنى قابل للضبط (افتراضي: 5 حسابات)

---

### 5️⃣ منطق استبدال الأكونت التالف

#### الآلية الكاملة:
1. ✅ وسم الأكونت الحالي كـ `damaged`
2. ✅ البحث عن أكونت بديل متاح (نفس المنتج)
3. ✅ ربط البديل بالأوردر وتغيير حالته إلى `used`
4. ✅ تحديث الأوردر بمعلومات الاستبدال
5. ✅ كتابة سجل تدقيق (Audit Log)

#### الاستخدام:
```javascript
import { replaceDeamagedAccount } from './enhanced-features.js';

try {
    const result = await replaceDeamagedAccount(
        db,
        'order123',
        'الحساب لا يعمل - طلب من العميل',
        'moderator456'
    );
    
    if (result.success) {
        console.log('تم الاستبدال بنجاح');
        console.log('الحساب القديم:', result.oldAccountId);
        console.log('الحساب الجديد:', result.newAccountId);
    }
} catch (error) {
    console.error('فشل الاستبدال:', error.message);
}
```

#### الحقول المضافة للأوردر:
```javascript
{
    damaged_account_id: 'acc_old_123',
    replacement_account_id: 'acc_new_456',
    replacement_timestamp: Timestamp,
    replacement_by: 'moderator456',
    replacement_reason: 'الحساب لا يعمل'
}
```

#### سجل التدقيق (Audit Log):
```javascript
{
    action: 'account_replace_due_to_damage',
    order_id: 'order123',
    product_id: 'Netflix',
    old_account_id: 'acc_old_123',
    new_account_id: 'acc_new_456',
    moderator_id: 'moderator456',
    note: 'الحساب لا يعمل',
    timestamp: Timestamp
}
```

#### حالات الحافة:
- ❌ **لا يوجد بديل متاح**: رسالة واضحة ومنع العملية
- ⚠️ **تعارض استخدام بديل**: استخدام معاملة (Transaction) لضمان الاتساق
- ⚠️ **أوردر بلا account_id**: إخفاء زر الاستبدال

---

### 6️⃣ صفحة تفاصيل الأوردر المحسنة

#### البطاقات الرئيسية:
1. **ملخص الأوردر**:
   - رقم الأوردر
   - العميل
   - المنتج
   - السعر
   - التاريخ
   - طريقة الدفع
   - الحالة (مؤكد/قيد المراجعة)

2. **الحساب المرتبط**:
   - البريد الإلكتروني
   - كلمة المرور (مع إخفاء/إظهار)
   - حالة الحساب
   - زر "تعيين كـ تالف واستبدال"
   - عرض الحساب التالف (إذا وجد)

3. **معلومات الاشتراك**:
   - تاريخ البداية/الانتهاء
   - حالة التجديد
   - أيام متبقية
   - تنبيه إذا كان التجديد قريب

4. **السجل الزمني (Timeline)**:
   - إنشاء الأوردر
   - ربط الحساب
   - استبدال الحساب (إذا حدث)
   - سجلات التدقيق

#### الإجراءات المتاحة:
- 🖨️ طباعة
- 📄 تصدير PDF
- ✏️ تعديل سريع
- 🔗 الانتقال لسجل العميل

#### الوصول للصفحة:
```html
<a href="order-details.html?id=ORDER_ID">عرض التفاصيل</a>
```

---

### 7️⃣ الصفحة الرئيسية المحسنة

#### KPIs الرئيسية:
- 👥 الاشتراكات النشطة
- 💰 مبيعات اليوم/الأسبوع
- 🔔 تجديدات معلقة (خلال 48 ساعة)
- ✅ أكونتات متاحة/مستخدمة/تالفة
- 💸 مصروفات اليوم/الشهر

#### المخططات:
1. **مخطط خطي**: المبيعات آخر 7 أيام
2. **مخطط أعمدة**: أفضل 5 منتجات

#### لوحة التنبيهات:
- ⚠️ اشتراكات قرب الانتهاء
- 🚨 نقص الأكونتات المتاحة
- 📋 أوردرات غير مؤكدة

#### روابط سريعة:
- سجل المبيعات
- الأكونتات
- المصروفات
- الشيفتات

---

## 🔧 التكامل مع النظام الموجود

### الخطوة 1: إضافة الملفات الجديدة

```html
<!-- في ملف index.html -->
<script type="module" src="enhanced-features.js"></script>
<script type="module" src="enhanced-integration.js"></script>
```

### الخطوة 2: التهيئة في app.js

```javascript
import { initializeEnhancedFeatures } from './enhanced-integration.js';

// بعد تحميل البيانات من Firebase
initializeEnhancedFeatures(db, allSales, allExpenses, allAccounts, allProducts);
```

### الخطوة 3: دمج الأقسام الجديدة

قم بنسخ محتوى `enhanced-sections.html` وإضافته إلى `index.html` في المكان المناسب.

---

## 📋 موديل البيانات المحدث

### جدول `orders` (sales):
```javascript
{
    id: string,
    customer: string,
    product: string,
    price: number,
    created_at: ISO string,
    payment_method: string,
    status: string,
    account_id: string,
    damaged_account_id: string | null,
    replacement_account_id: string | null,
    replacement_timestamp: Timestamp | null,
    replacement_by: string | null,
    replacement_reason: string | null,
    subscription_start: ISO string,
    subscription_end: ISO string,
    renewalStatus: string,
    isConfirmed: boolean,
    notes: string
}
```

### جدول `accounts`:
```javascript
{
    id: string,
    product_id: string,
    email: string,
    password: string,
    status: 'available' | 'used' | 'damaged',
    created_at: Timestamp,
    updated_at: Timestamp,
    assigned_to_order: string | null,
    assigned_at: Timestamp | null,
    damaged_at: Timestamp | null,
    damage_reason: string | null,
    damaged_by: string | null
}
```

### جدول `expenses`:
```javascript
{
    id: string,
    date: ISO string, // إلزامي
    amount: number,
    category: 'ads' | 'operational' | 'renewal' | 'other',
    product_id: string,
    note: string,
    created_at: Timestamp,
    updated_at: Timestamp
}
```

### جدول `products`:
```javascript
{
    id: string,
    name: string,
    price_config: object,
    is_active: boolean
}
```

### جدول `audit_logs` (جديد):
```javascript
{
    id: string,
    action: string,
    order_id: string,
    product_id: string,
    old_account_id: string,
    new_account_id: string,
    moderator_id: string,
    note: string,
    timestamp: Timestamp
}
```

---

## 🔍 الفهارس المطلوبة (Firestore Indexes)

لضمان الأداء الأمثل، أنشئ الفهارس التالية في Firebase Console:

```javascript
// orders collection
{
    collection: 'sales',
    fields: [
        { fieldPath: 'created_at', order: 'DESCENDING' },
        { fieldPath: 'product', order: 'ASCENDING' }
    ]
}

// accounts collection
{
    collection: 'accounts',
    fields: [
        { fieldPath: 'product_id', order: 'ASCENDING' },
        { fieldPath: 'status', order: 'ASCENDING' },
        { fieldPath: 'created_at', order: 'ASCENDING' }
    ]
}

// expenses collection
{
    collection: 'expenses',
    fields: [
        { fieldPath: 'date', order: 'DESCENDING' },
        { fieldPath: 'product_id', order: 'ASCENDING' }
    ]
}

// audit_logs collection
{
    collection: 'audit_logs',
    fields: [
        { fieldPath: 'order_id', order: 'ASCENDING' },
        { fieldPath: 'timestamp', order: 'DESCENDING' }
    ]
}
```

---

## 🎨 الواجهات والتصميم

### المبادئ المتبعة:
- ✅ واجهة عربية كاملة (RTL)
- ✅ استخدام خط Tajawal
- ✅ ألوان متسقة مع النظام الموجود
- ✅ أيقونات Font Awesome
- ✅ تصميم متجاوب (Responsive)
- ✅ دعم الوضع الليلي

### الألوان الرئيسية:
```css
--primary-color: #6366f1;
--success-color: #10b981;
--danger-color: #ef4444;
--warning-color: #f59e0b;
--info-color: #3b82f6;
```

---

## 📤 التصدير والتقارير

### الصيغ المدعومة:
1. **CSV**: للاستخدام في Excel والبرامج المكتبية
2. **Excel (XLSX)**: ملفات احترافية مع تنسيق
3. **PDF**: (قريباً)

### الوظائف المتاحة:
```javascript
import { exportToExcel, exportShiftReportToCSV } from './enhanced-features.js';

// تصدير بيانات عامة
exportToExcel(data, 'تقرير المبيعات', 'Sales');

// تصدير تقرير شيفت
exportShiftReportToCSV(shiftStats, '2025-11-11');
```

---

## 🔐 الأمان والصلاحيات

### التحقق من الصلاحيات:
```javascript
import { hasPermission, PERMISSIONS } from './auth.js';

if (hasPermission(PERMISSIONS.EDIT_ACCOUNT)) {
    // السماح بالتعديل
}

if (hasPermission(PERMISSIONS.DELETE_ACCOUNT)) {
    // السماح بالحذف
}
```

### العمليات الحساسة:
- ✅ استبدال الأكونت التالف: يتطلب صلاحية `EDIT_ACCOUNT`
- ✅ حذف المصروفات: يتطلب صلاحية `DELETE_EXPENSE`
- ✅ تعديل الأوردرات: يتطلب صلاحية `EDIT_SALE`

---

## 🐛 معالجة الأخطاء

### الأخطاء الشائعة:

#### 1. لا يوجد بديل متاح
```javascript
Error: لا يوجد حساب بديل متاح لهذا المنتج

الحل: إضافة المزيد من الأكونتات المتاحة للمنتج
```

#### 2. تاريخ غير صالح
```javascript
Error: التاريخ المدخل بعيد جداً في المستقبل

الحل: التأكد من صحة التاريخ
```

#### 3. فشل المعاملة (Transaction)
```javascript
Error: تعارض في الاستخدام

الحل: إعادة المحاولة تلقائياً
```

---

## 📊 الأداء والتحسينات

### التحسينات المطبقة:
- ✅ استخدام Firestore Indexes للاستعلامات السريعة
- ✅ معاملات (Transactions) لضمان اتساق البيانات
- ✅ تحميل كسول (Lazy Loading) للمخططات
- ✅ Skeleton UI عند الانتظار
- ✅ تخزين مؤقت للبيانات

### نصائح للأداء:
1. استخدم فلاتر التاريخ لتقليل حجم البيانات المحملة
2. تجنب تحميل جميع البيانات مرة واحدة
3. استخدم Pagination للجداول الكبيرة
4. قم بتحديث الفهارس بانتظام

---

## 🧪 الاختبار

### سيناريوهات الاختبار:

#### 1. استبدال أكونت تالف:
```
✓ اختبار مع أكونت بديل متاح
✓ اختبار بدون أكونت بديل
✓ اختبار مع عدة محاولات متزامنة
✓ التحقق من سجل التدقيق
```

#### 2. إضافة مصروف:
```
✓ إضافة مصروف بتاريخ صحيح
✓ إضافة مصروف بتاريخ قديم
✓ إضافة مصروف بتاريخ مستقبلي
✓ التحقق من الفلترة والترتيب
```

#### 3. إحصائيات الشيفتات:
```
✓ حساب شيفت يحتوي على أوردرات
✓ حساب شيفت فارغ
✓ حساب تفاصيل المنتجات
✓ التصدير إلى CSV/Excel
```

---

## 📝 قائمة التحقق (Checklist)

### قبل النشر:
- [ ] التأكد من إضافة جميع الفهارس في Firestore
- [ ] اختبار استبدال الأكونت التالف
- [ ] اختبار التصدير إلى CSV/Excel
- [ ] التأكد من عمل جميع الفلاتر
- [ ] اختبار الواجهة على الموبايل
- [ ] التحقق من الأذونات والصلاحيات
- [ ] مراجعة سجلات التدقيق
- [ ] اختبار السيناريوهات الحدية

---

## 🤝 الدعم والمساعدة

للأسئلة والاستفسارات:
- 📧 support@subpro.com
- 📚 [التوثيق الكامل](README.md)
- 🐛 [الإبلاغ عن مشكلة](ISSUES.md)

---

## 🎉 الخلاصة

تم تطوير نظام شامل ومتكامل يوفر:
- ✅ إدارة دقيقة للشيفتات اليومية
- ✅ تتبع المصروفات مع فلاتر متقدمة
- ✅ إحصائيات تفصيلية لكل منتج
- ✅ منطق استبدال الأكونت التالف تلقائياً
- ✅ واجهات محسنة وسهلة الاستخدام
- ✅ تقارير قابلة للتصدير
- ✅ تنبيهات ذكية

**مبروك! نظام SubPro أصبح أكثر احترافية وقوة.** 🚀

---

© 2025 SubPro Dashboard - جميع الحقوق محفوظة
